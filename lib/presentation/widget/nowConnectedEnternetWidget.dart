import 'package:flutter/material.dart';

class Noconnectedenternetwidget extends StatelessWidget{
    
     
       @override
       Widget build(BuildContext context) {
         return const Text("no connected ");

       }
}